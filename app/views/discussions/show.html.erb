<section class='view'>
  <h2 data-role="discussion-name"><%= @discussion.name %></h2>
  <p class="share">Share this link to begin a discussion: <%= link_to discussion_url(@discussion), @discussion %></p>
  <%= render @discussion.subject %>
</section>

<section class='chat'>
  <%= render 'chat' %>
</section>
